# Byte-compiled / cache
__pycache__/
*.pyc

# Local virtual environment (see README > Installation / Deployment)
.venv/
venv/

# Generated data (large binary outputs, regenerate with the script)
datasets/mseed/*.mseed
datasets/mseed/*.json
datasets/sonifications/*.wav
datasets/plot/*.png
datasets/maps/*.png

# Keep the folder structure in git even though contents are ignored
!datasets/mseed/.gitkeep
!datasets/sonifications/.gitkeep
!datasets/plot/.gitkeep
!datasets/maps/.gitkeep

# Too large for GitHub (>100MB) -- kept locally only, never tracked
docs/Sonification.zip

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
